<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
             "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.skhu.mapper.ArticleMapper">

	<select id="findAllByBoard" resultType="Article">
		SELECT a.*,u.name "userName" FROM article a LEFT JOIN user u ON a.art_u_id=u.id WHERE a.art_b_id = #{art_b_id}
	</select>
	
	<select id="findOne" resultType="Article">
		SELECT * FROM article WHERE id = #{id}
	</select>
	
	<insert id="insert" useGeneratedKeys="true" keyProperty="id">
	  INSERT article (title, content, art_u_id, post_date, art_f_id, art_b_id)
	  VALUES (#{title}, #{content}, #{art_u_id}, now(), null, #{art_b_id})
	</insert>
	
	<!-- 
	<update id="update">
	  UPDATE Student SET 
	    studentNumber = #{studentNumber}, 
	    name = #{name}, 
	    departmentId = #{departmentId}, 
	    year = #{year} 
	  WHERE id = #{id}
	</update>
	
	<delete id="delete">
	  DELETE FROM article WHERE id = #{id}
	</delete>
	-->

</mapper>